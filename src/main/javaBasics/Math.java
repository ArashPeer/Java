import java.util.Arrays;

public class Math {

    public static void main(String[] args) {
        System.out.println(java.lang.Math.abs(-10));
      double max  = java.lang.Math.max(3.6, 10.11);
        System.out.println(max);
        System.out.println(java.lang.Math.min(12,23));

        double maxNum = java.lang.Math.max(2.44, 5.22);
        String name = "Ali Mohammad";
        System.out.println("This is "+ name + " PartyID# " + maxNum);
        System.out.println((int)java.lang.Math.pow(2,10));
        //the pow num to the left will by multiply by itself and the number of multiplicaiton by the number on the right
        System.out.println((int)java.lang.Math.sqrt(3));

    }
}
